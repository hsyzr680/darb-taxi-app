## Packages
leaflet | Core mapping library
react-leaflet | React components for Leaflet maps
framer-motion | Smooth animations for UI elements
lucide-react | Icon set
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility for merging Tailwind CSS classes safely

## Notes
Leaflet requires its CSS to be imported in index.css
Map tiles will use OpenStreetMap (free)
Rider dashboard needs geolocation access
Driver dashboard polls for active trips
